# @package model.audio_encoder
# Shared audio encoder configuration
n_mels: 80
d_model: 512
nhead: 8
num_layers: 6
dim_feedforward: 2048
dropout: 0.1
n_structure_features: 8  # 6 energy features + section_id + section_progress
